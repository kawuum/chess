#ifndef CONSTS_HPP
#define CONSTS_HPP

#include <wx/wx.h>

const wxColor BG_GREY = wxColor(80,80,80);
const wxColor BG_WHITE = wxColor(255, 255, 255);
const wxColor BG_RED = wxColor(255, 0, 0);
const wxColor BG_BLUE = wxColor(0, 0, 255);

#endif
